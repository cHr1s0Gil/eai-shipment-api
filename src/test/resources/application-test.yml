spring:
  datasource:
    url: jdbc:h2:mem:eai-shipment-testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      "[hibernate.format_sql]": true
      
security:
  api-key:
    value: api-key-test